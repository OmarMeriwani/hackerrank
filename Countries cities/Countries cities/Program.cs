using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Countries_cities
{
    class Program
    {
        
        static void Main(string[] args)
        {
            string[] cou = { "Iraq", "Egypt", "Syria",  "Iran", "Afghanistan"};
            int[] couID = { 1, 2, 3, 4 };
            string[] ci = { "Baghdad", "Basra", "Hilla", "Samawa", "Cairo", "Alexandria", "Dumiat", "Damascus", "Aleppo", "Tehran", "Mahabad" };
            int[] ciId = { 1,1,1,1,2,2,2,3,3,4,4};
            Dictionary<string, int> countries = new Dictionary<string, int>();
            Dictionary<string, int> cities = new Dictionary<string, int>();
            for (int i = 0 ; i < cou.Length; i++)
        }
    }
}
